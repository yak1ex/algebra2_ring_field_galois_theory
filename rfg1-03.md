# 代数学2 環と体とガロア理論

## 第1章 環論の基本

### 1.3 部分環とイデアル

#### 命題1.3.16

$$
\psi'(f(x))=\sum_{i_c,\cdots,i_n}\psi'(c_{i_1,\cdots,i_n})\psi(x_1)^{i_1}\cdots\psi(x_n)^{i_n}\\
=\sum_{i_c,\cdots,i_n}\phi(c_{i_1,\cdots,i_n})\psi'(x_1)^{i_1}\cdots\psi'(x_n)^{i_n}
$$

$\psi'(c_{i_1,\cdots,i_n})=\phi(c_{i_1,\cdots,i_n})$ であることは $k$ 準同型の定義(定義1.3.15(2)) において
$A=k[x],B=A,\psi=\phi,f=\psi,\phi$ を包含写像としたときの状況にあたり $k$ の元である $c_{i_1,\cdots,i_n}$ の行き先は $\phi$ と一致する必要があることからわかる。

これは $k[x]$ を $k$ 上の代数と考える時に $k\to k[x]$ は包含写像を前提としている？

#### 命題1.3.17

条件側で効いてるところはどこ？

* $k$ は体
* $f(x)\in k[x]\setminus k$ ($k$ 除く必要ある？)

#### 例1.3.19

「この場合」不要では？
↓
この場合、 $k=\mathbb{Z}$ なので環準同型と $k$ 準同型は同じである。

くらいか。

#### 命題1.3.20

部分 $k$ 代数の定義 p.18 定義1.3.15(3) と議論が整合してなくない？

ref. https://detail.chiebukuro.yahoo.co.jp/qa/question_detail/q10203137095

###### (2)

$S$ を含む $A$ の部分 $k$ 代数 $C$ は $A$ における $k$ の像を含む

$C$ は $k$ 代数で $f:C→A$ で単射な $k$ 準同型を持つ。
$(A,\psi),(C,\phi)$ とすると定義1.3.15(2)から $f\circ\phi=\psi$ で
$f$ が単射なので $\psi(k)$ にあたる像が $C$ に含まれていなければならない。
※厳密な表記じゃない

$k$ 代数といったときにそもそも環なので和積について閉じてるのは当然。
$S$ は $k$ を含むとは限らないので $k$ を含んでることを言う必要がある。

(1)から(2)を導くこともできそう。
$S\subset C$ なので(1)より $k[S]$ は $C$ の部分 $k$ 代数。
よって $k[S]$ から $C$ への単射があるので $k[S]\subset C$ 、というには包含写像である必要がありそう

#### p.21 注

$k$ 代数は積がある。加群はスカラー倍のみで積は考えない。

##### 系1.3.23

この $\phi$ が命題1.3.20の上(前提)の $f(x_1,\cdots,x_n)$ を $f(a_1,\cdots,a_n)$ に置き換える操作に相当する、はず。 $\phi(f(x_1,\cdots,x_n))=f(\phi(x_1),\cdots,\phi(x_n))=f(a_1,\cdots,a_n)$
命題1.3.16から唯一性が言えるので像が $k[S]$ になる、でいいのかな。

### 演習問題 1.3

#### 1.3.1

$f(x,y,z)=x^2+yz$ に $x=t^2+1, y=1-t, z=3t+2$ を代入して整理
$f(t^2+1,1-t,3t+2)=(t^2+1)^2+(1-t)(3t+2)=t^4+2t^2+1-3t^2+t+2=t^4-t^2+t+3$

```python
from sympy import simplify
from sympy.abc import x, y, z, t
simplify((x ** 2 + y * z).subs(x, t**2+1).subs(y, 1-t).subs(z, 3*t+2))
# t**4 - t**2 + t + 3
```

#### 1.3.2

##### 1

$A=\mathbb{C}\lbrack x\rbrack, B=\lbrace a_0+a_2x^2+a_3x^3+\cdots+a_nx^n\mid a_0,a_2,a_3,\cdots,a_n\in\mathbb{C}\rbrace$
$B$ が $A$ の部分環であることを証明

加法で閉じていることは多項式の加法の定義から明らか、係数の符号を反転させれば加法逆元となるので加法逆元で閉じていることも明らか、
加法単位元 $0\in B$ も明らかなので $B$ は $A$ の部分群である。

$B$ の元は定数項と $2$ 次以上の項で表せる。
$B$ の任意の $2$ 元を $c=c_0+C(x),d=d_0+D(x)$ ( $C(x),D(x)$ は $2$ 次以上の項) として、
$cd=(c_0+C(x))(d_0+D(x))=c_0d_0+c_0D(x)+d_0C(x)+C(x)D(x)$ である。
$c_0d_0$ は定数項、
$c_0D(x)$ は $0(c_0=0\text{のとき})$ あるいは $2$ 次以上の項、
$d_0C(x)$ は $0(d_0=0\text{のとき})$ あるいは $2$ 次以上の項、
$C(x)D(x)$ は $4$ 次以上の項となるので $cd\in B$ である。
よって乗法でも閉じており、乗法単位元 $1\in B$ も明らかなので $B$ は $A$ の部分環である。

##### 2

$A=\mathbb{C}\lbrack x\rbrack, B=\lbrace a_0+a_1x+a_3x^3+\cdots+a_nx^n\mid a_0,a_1,a_3,\cdots,a_n\in\mathbb{C}\rbrace$
$B$ が $A$ の部分環ではないことを証明

$1+x\in B$ で $(1+x)(1+x)=1+2x+x^2\notin B$ となり乗法で閉じていないため $B$ は $A$ の部分環ではない。

#### 1.3.3

$A=\lbrace a/2^n\in\mathbb{Q}\mid a\in\mathbb{Z},n\ge0\rbrace$
$A$ が $\mathbb{Q}$ の部分環であることを証明

明記されていないが $n\in\mathbb{Z}$ を前提とする。

$a=0\in\mathbb{Z}, n\ge0$ で $\frac{a}{2^n}=0\in A$ なので $A$ は加法単位元を含む。
$a$ の符号を反転させれば加法逆元となるので $A$ は加法逆元で閉じている。
$a=1\in\mathbb{Z}, n=0\ge0$ のとき $\frac{a}{2}=1\in A$ なので $A$ は乗法単位元を含む。
$A$ の $2$ 元を $\alpha_1=\frac{a_1}{2^{n_1}},\alpha_2=\frac{a_2}{2^{n_2}}, a_1,a_2,n_1,n_2\in\mathbb{Z}, n_1,n_2\ge0$ とする。
$n_1\ge n_2$ として一般性を失わない。
$\alpha_1+\alpha_2=\frac{a_1}{2^{n_1}}+\frac{a_2}{2^{n_2}}=\frac{a_1+2^{n_1-n_2}a_2}{2^n_1}$ で
$n_1\ge n_2$ だから $a_1+2^{n_1-n_2}\in\mathbb{Z}$ なので $\alpha_1+\alpha_2\in A$ である。よって $A$ は加法で閉じている。
$\alpha_1\alpha_2=\frac{a_1}{2^{n_1}}\frac{a_2}{2^{n_2}}=\frac{a_1a_2}{2^{n_1+n_2}}$ で $a_1a_2\in\mathbb{Z}$ なので $\alpha_1\alpha_2\in A$ である。
よって $A$ は乗法でも閉じている。

以上より、 $A$ は $\mathbb{Q}$ の部分環である。

#### 1.3.4

環 $\mathbb{Z}$ の自己同型は恒等写像だけであることを証明

$\mathbb{Z}$ の自己同型を $\phi:\mathbb{Z}\to\mathbb{Z}$ とする。
$\phi$ は環の準同型だから $\phi(0)=\phi(0+0)=\phi(0)+\phi(0)$ より $\phi(0)=0$ である。
$\phi$ は環の準同型だから $\phi(1)=1$ である。
$n=k$ で $\phi(k)=k$ とすると $\phi(k+1)=\phi(k)+\phi(1)=k+1$ となるので帰納法より $n\ge0$ で $\phi(n)=n$ である。 
$n\ge0$ として $0=\phi(0)=\phi(n+(-n))=\phi(n)+\phi(-n)$ より $\phi(-n)=-\phi(n)=-n$ である。
よって任意の $n\in\mathbb{Z}$ で $\phi(n)=n$ となるため、 $\mathbb{Z}$ の自己同型は恒等写像だけである。

#### 1.3.5

写像 $\phi:\mathbb{Z}\to\mathbb{Z}$ で $\phi(x+y)=\phi(x)+\phi(y), \phi(xy)=\phi(x)\phi(y)$ だが $\phi(1)\neq1$ であるものはあるか？

$\phi(x)=\phi(x+0)=\phi(x)+\phi(0)$ より $\phi(0)=0$ である。
$\phi(1)=\phi(1\cdot1)=\phi(1)\phi(1)$ より $\phi(1)(\phi(1)-1)=0$ で $\mathbb{Z}$ は整域なので $\phi(1)=0$ または $\phi(1)=1$ である。
よって $phi(1)\neq1$ であれば $\phi(1)=0$ である。
$\phi(1+1)=\phi(1+1)=2\phi(1)=0$ で任意の $n\in\mathbb{Z}$ について同様なので題意を満たす写像は存在し $phi(x)=0$ のみである。 

#### 1.3.6

$\mathbb{C}$ 代数の準同型の例を $5$ つ挙げよ

* $\mathbb{C}\lbrack x\rbrack\ni f(x)\mapsto \overline{f(x)}\in\mathbb{C}\lbrack x\rbrack$ ※複素共役
* $\mathbb{C}\lbrack x\rbrack\ni f(x)\mapsto f(0)\in\mathbb{C}$ ※定数項取り出し
* $\mathbb{C}\lbrack x\rbrack\ni f(x)\mapsto f(y)\in\mathbb{C}\lbrack y\rbrack$ ※単純に文字を置き換え
* $\mathbb{C}\lbrack x,y\rbrack\ni f(x,y)\mapsto f(x,c)\in\mathbb{C}\lbrack x\rbrack$ ※ 定数の代入
* $\mathbb{C}\lbrack x,y\rbrack\ni f(x,y)\mapsto f(s+t,st)\in\mathbb{C}\lbrack s,t\rbrack$ ※ 多項式の代入

#### 1.3.7

$A=\mathbb{C}\lbrack x,y\rbrack$ で $x^2-x^5y$ がイデアル $I=(x^2-y^3,x^5-y^2)$ の元であることを証明

$x^2-x^5y=(x^2-y^3)+(-y)(x^5-y^2)$

#### 1.3.8

##### 1

$\phi:\mathbb{C}\lbrack x,y\rbrack\to\mathbb{C}\lbrack t\rbrack$ を $\mathbb{C}$ 代数の準同型で $\phi(x)=t^3, \phi(y)=t^4$ であるものとする。
このとき $\mathrm{Ker}(\phi)$ の生成系を求めよ。

明らかに $x^4-y^3\in\mathrm{Ker}(\phi)$ なので、 $\mathrm{Ker}(\phi)\supset(x^4-y^3)$ である。
$f(x,y)\in\mathrm{Ker}{\phi}$ とする。 $x^4-y^3$ は $y$ の多項式としては $3$ 次元で最高次の係数は単元なので命題1.2.13より
$f(x,y)=g(x,y)(x^4-y^3)+h_1(x)y^2+h_2(x)y+h_3(x)$
とかける。
$f(t^3,t^4)=h_1(t^3)t^8+h_2(t^3)t^4+h_3(t^3)=0$ で $h_1(t^3)t^8$ の項の次数は $3$ で割った余りは $2$ 、 $h_2(t^3)t^4$ の項の次数は $3$ で割った余りは $1$ 、  $h_3(t^3)$ の項の次数は $3$ で割った余りは $0$ 、となるため $h_1(x)=h_2(x)=h_3(x)=0$ である。よって、 $f(x,y)=g(x,y)(x^3-y^4)$ となり $f(x,y)\in(x^3-y^4)$ である。したがって $\mathrm{Ker}(\phi)\subset(x^3-y^4)$ となる。
以上より、 $\mathrm{Ker}(\phi)=(x^3-y^4)$

##### 2

$\phi:\mathbb{C}\lbrack x,y,z\rbrack\to\mathbb{C}\lbrack t,s\rbrack$ を $\mathbb{C}$ 代数の準同型で $\phi(x)=t^2, \phi(y)=ts, \phi(z)=s^2$ であるものとする。
このとき $\mathrm{Ker}(\phi)$ の生成系を求めよ。

##### 3

$\phi:\mathbb{C}\lbrack x,y,z\rbrack\to\mathbb{C}\lbrack t\rbrack$ を $\mathbb{C}$ 代数の準同型で $\phi(x)=t^3, \phi(y)=t^4, \phi(z)=t^5$ であるものとする。
このとき $\mathrm{Ker}(\phi)$ の生成系を求めよ。

##### 4

$\phi:\mathbb{C}\lbrack x,y,z\rbrack\to\mathbb{C}\lbrack t\rbrack$ を $\mathbb{C}$ 代数の準同型で $\phi(x)=t^4, \phi(y)=t^5, \phi(z)=t^7$ であるものとする。
このとき $\mathrm{Ker}(\phi)$ の生成系を求めよ。

#### 1.3.9

$A=\mathbb{C}\lbrack x,y,z\rbrack, B=\mathbb{C}\lbrack x,z\rbrack, I=(x^2-y^3,y^3-z^5)\subset A\Rightarrow I\cap B=(x^2-z^5)B$ を証明

#### 1.3.10

$A=\mathbb{C}\lbrack x,y\rbrack$

##### 1

$a,b,c,d\in\mathbb{C},\phi(x)=ax+by,\phi(y)=cx+dy$ のとき $\phi:A\to A$ が $\mathbb{C}$ 代数の同型 $\Leftrightarrow ad-bc\neq0$ を証明

##### 2

$f(x)\in\mathbb{C}\lbrack x\rbrack, \phi(x)=x, \phi(y)=f(x)+y, \phi:A\to A$ が $\mathbb{C}$ 代数の同型を証明 

#### 1.3.11

$B=\mathbb{C}\lbrack t\rbrack, f(t),g(t)\in B\setminus\lbrace0\rbrace\Rightarrow \deg f(g(t))=\deg f(x)\deg g(t)$ を証明

#### 1.3.12

$A=\mathbb{C}\lbrack x\rbrack, \phi:A\to A$ が $\mathbb{C}$ 代数としての自己同型 $\Rightarrow\exists a\in\mathbb{C}\setminus\lbrace0\rbrace, b\in\mathbb{C},\phi(x)=ax+b$ 

#### 1.3.13

$A=\mathbb{Z}, B=\mathbb{Q}, I=2\mathbb{Z}$ のとき $IB\cap A$ はなにか？

$\frac{1}{2}\in B=\mathbb{Q}$ なので $\mathbb{Z}\subset IB$ だから $IB\cap A=A\supset B$

#### 1.3.14

$A=\mathbb{Z}, B=\mathbb{Z}\lbrack x\rbrack, I=(x^2+1)B\Rightarrow I\cap A=(0)$

$b=b_0+b_1x+b_2x^2+\cdots+b_nx^n\in B\quad(n\le0)$ として $I$ の任意の元は
$(x^2+1)b=b_0+b_1x+(b_0+b_2)x^2+\cdots+(b_{n-2}+b_n)x^n+b_{n-1}x^{n+1}+b_nx^{n+2}$ として表せる。
$(x^2+1)b\in I\cap A$ とすると $(x^2+1)b\in A$ なので
$b_1,b_0+b_2,\cdots,b_{n-2}+b_n,b_{n-1},b_n$ の全てが $0$ である。
$b_{n-1},b_n$$ が $0$ であり $b_{n-2}+b_n,b_{n-3}+b_{n-1}$ が $0$ であることから $b_{n-2},b_{n-3}$ も $0$ となりこれを繰り返すことで全ての $b_n$ が $0$ となる。
よって $(x^2+1)b=0$ となるため、 $I=\lbrace0\rbrace=(0)$ である。

※形式的に厳密にしたい

#### 1.3.15

$A=\mathbb{C}\lbrack x,y\rbrack, B=\mathbb{C}\lbrack x^2,y\rbrack,I=(x^2-y^2)A$ で $I\cap A$ の生成系を求めよ。

#### 1.3.16

$A=\mathbb{C}\lbrack x,y\rbrack, I=(x^2+y+1,x+y^2),J=(x,y)$ のとき $I+J$ はなにか？

#### 1.3.17

$x=(x_1,\cdots,x_n),y=(y_1,\cdots,y_m)$ を変数として、 $A=\mathbb{C}\lbrack x\rbrack,B=\mathbb{C}\lbrack x,y\rbrack$
$l_1\le n,l_2\le m,I=Bx_1+\cdots+Bx_{l_1}+By_1+\cdots+By_{l_2}\Rightarrow I\cap A=Ax_1+\cdots+A_{x_{l_1}}$

#### 1.3.18

$x=(x_1,\cdots,x_n),y=(y_1,\cdots,y_m)$ を変数として、 $A=\mathbb{C}\lbrack x,y\rbrack,I=(x_1,\cdots,x_n)$
$1\le i_1,\cdots,i_t\le n, 1\le j_1,\cdots,j_s\le m$ に対し、 $D=\partial_{x_{i_1}}\cdots\partial_{x_{i_t}}\partial_{y_{j_1}}\cdots\partial_{y_{j_s}}$
$l\ge t, f\in I^l\Rightarrow Df\in I^{l-t}$

#### 1.3.19

$A=\mathbb{C}\lbrack x,y\rbrack, I=(x,y), J=(x), K=(y)$
イデアルの生成系を求めよ

##### 1

$I^2\cap J$

##### 2

$I^3\cap J\cap K$
